$('button').click(function() {
    $('.button-container').append('<h1>You have Clicked The Button</h1>');
});

// JSX is HTML inside the JavaScript

render : function () {
    return (
        <div id="divOne">Hello World</div>
    );
}
